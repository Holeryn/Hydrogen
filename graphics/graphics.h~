#ifndef GRAPHICS
#define GRAPHICS

#define NUM_SPHERES 1000000

#define RAGGIO_SFERA     0.8f    /* raggio della sfera "grande" (disposizione) */
#define RAGGIO_PALLINA   0.4f    /* raggio di ogni singola pallina */


#define SENSIBILITA_MOUSE 0.15f
#define VELOCITA_MOVIMENTO 8.0f


typedef struct sphere {
  float x,y,z;
  float r,g,b;
} Sphere; 

/*
void start_opengl();
void add_sphere(x,y,z,...);
void rm_sphere(x,y,z,...);
void graph();
*/

void add_sphere(float x,
		float y,
		float z,
		float r,
		float g,
		float b);

void start_opengl(int argc,
		  char **argv);

#endif
